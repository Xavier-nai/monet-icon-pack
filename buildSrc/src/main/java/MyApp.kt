@file:Suppress("unused")

object MyApp {
    // Internal namespace for generated R/BuildConfig and source packages.
    // Kept as the original value so Kotlin sources need no package moves.
    const val appId = "dev.jahir.blueprint.app"

    // Installed package id (app identity on device / stores).
    const val applicationId = "com.monetcarlos2026.iconpack"

    const val version = 1
    const val versionName = "1.0.0"
}
